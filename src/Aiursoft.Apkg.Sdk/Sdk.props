<Project>
  <!-- Declare custom item types so MSBuild recognizes them before the SDK runs -->
  <ItemGroup>
    <DependencyCheckSource />
    <DpkgTrigger />
    <PrebuildCommand />
    <TestCommand />
  </ItemGroup>
</Project>
